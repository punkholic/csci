
/* 
our normal datatype can only hold a singular value of its own kind
here it can only hold 12
so to store many kind of values in same variable name we can use array
*/
a = 12;
// we can create array as:

newArray1 = [1,2,3];
// modifying array index:
newArray1[0] =43;

// unlike c array we can store any kind of datatype in js array like:
newArray = ["ram", 1, true, 4.5]
// we can also print entire array :
console.log(newArray)


   
