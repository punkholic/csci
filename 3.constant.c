#include <stdio.h>
void main(){
    const int PI = 12;
    /*
    constant values can only be defined once
    and cannot be changed again like:
    PI =43; this will throw error
    */
    printf("%d", PI);
}